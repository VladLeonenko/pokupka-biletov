/**
 * GlobalPreloader - компонент для отображения загрузчика при первом посещении
 * Временно отключен (не работает как ожидалось)
 */

// Заглушки для обратной совместимости
export function GlobalPreloader() {
  return null;
}

export function useGlobalPreloader() {
  return false;
}
